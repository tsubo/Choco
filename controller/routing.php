<?php
/**
 * Define your routings here if you need.
 * map resource UIRs to callback function for specific HTTP request methods.
 */

/*
$app->get('/foo', function () use ($app) {
	$app->render('page/foo/index.html.twig');
});

$app->get('/foo/:bar', function ($bar) use ($app) {
	$app->render("page/foo/${bar}.html.twig");
});

$app->get('/info', function () use ($app) {
	phpinfo();
})->name("info");
*/

